apiVersion: v1
kind: Service
metadata:
  name: java-product-service
spec:
  selector:
    app: java-product-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer